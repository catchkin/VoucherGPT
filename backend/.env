# Application Settings
APP_NAME=VoucherGPT
DEBUG=True
API_V1_STR=/api/v1
SERVER_HOST=http://localhost:8000

# JWT Settings
SECRET_KEY=your-super-secret-key-please-change-in-production-0123456789
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Database Settings
DB_USER=vouchergpt_user
DB_PASSWORD=blogcodi0318
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vouchergpt_test

# Database URL
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}
ASYNC_DATABASE_URL=postgresql+asyncpg://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# OpenAI Settings
OPENAI_API_KEY=your-openai-api-key-here
GPT_MODEL=gpt-4-1106-preview

# File Upload Settings
UPLOAD_DIR=./uploads
MAX_UPLOAD_SIZE=10485760  # 10MB in bytes
